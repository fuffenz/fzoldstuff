OldStuff is a WordPress plugin that can be configured to display a warning on old posts. You can configure it to display different messages depending on how old the post is.

The warning message can be styled with css.

Requires WP 2.8 or higher.

Defaults to English, can be translated.  Swedish language files are included.
